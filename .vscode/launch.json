{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C++ Program",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/program",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "externalConsole": true,   // ✅ separate terminal window
            "MIMode": "lldb",          // if using clang++ on Linux/macOS
            "preLaunchTask": "build program"
        }
    ]
}
